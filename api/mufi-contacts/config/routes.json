{
  "routes": [
    {
      "method": "GET",
      "path": "/mufi-contacts",
      "handler": "mufi-contacts.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/mufi-contacts/count",
      "handler": "mufi-contacts.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/mufi-contacts/:id",
      "handler": "mufi-contacts.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/mufi-contacts",
      "handler": "mufi-contacts.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/mufi-contacts/:id",
      "handler": "mufi-contacts.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/mufi-contacts/:id",
      "handler": "mufi-contacts.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
